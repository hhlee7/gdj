<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hashtag.mapper.MyMapper">
	<select id="selectBoardListAndTags" resultType="map">
		SELECT t.boardNo, t.title, IFNULL(GROUP_CONCAT(tag ORDER BY t.tag_no), '') tags
		FROM
		(SELECT b.board_no boardNo, b.title title, h.tag_no,h.tag tag
		FROM board b LEFT OUTER JOIN hashtag h
		ON b.board_no = h.board_no) t
		GROUP BY t.boardNo, t.title
		ORDER BY t.boardNo DESC;
	</select>
	
	<select id="selectTagsByBoard" parameterType="int" resultType="string">
		SELECT tag
		FROM hashtag
		WHERE board_no = #{boardNo}
	</select>
	
	<insert id="insertHashtag" parameterType="com.example.hashtag.dto.Hashtag">
		INSERT INTO hashtag (
			board_no, tag
		) VALUES (
			#{boardNo}, #{tag}
		)
	</insert>
</mapper>