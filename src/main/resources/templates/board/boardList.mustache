<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<title>boardList</title>
	<!-- Bootstrap CSS -->
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
<title>boardList</title>
</head>
<body>
	{{>layouts/header}}
		
		{{#msg}}
		<div>{{msg}}</div>
		{{/msg}}
	
		<table class="table">
		    <thead>
		    <tr>
		        <th scope="col">No</th>
		        <th scope="col">Title</th>
		        <th scope="col">Content</th>
		    </tr>
		    </thead>
		    <tbody>
		    {{#list}}
		        <tr>
		            <th>{{boardNo}}</th>
		            <td><a href="/board/boardOne?boardNo={{boardNo}}">{{boardTitle}}</a></td>
		            <td>{{boardContent}}</td>
		        </tr>
		    {{/list}}
		    </tbody>
		</table>
		
		<div>
			<!-- 첫 페이지가 아닐 때 -->
			{{^list.isFirst}}
			<a href="/board/boardList?currentPage={{prePage}}&word={{word}}">이전</a>
			{{/list.isFirst}}
			
			{{list.getNumber}}
			
			{{#list.hasNext}}
			<a href="/board/boardList?currentPage={{nextPage}}&word={{word}}">다음</a>
			{{/list.hasNext}}
		</div>
		
		<form action="/board/boardList" method="get">
			<input type="text" name="word">
			<button type="submit">검색</button>
		</form>
		
		<a href="/board/addBoard">New Board</a>
	{{>layouts/footer}}
</body>
</html>