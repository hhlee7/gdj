<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<!-- Bootstrap CSS -->
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
	<title>modifyMemberPw</title>
</head>
<body class="bg-light">

	{{>layouts/header}}

	<div class="container d-flex justify-content-center align-items-center"
		style="min-height: 80vh;">
		<div class="card p-4 shadow" style="width: 100%; max-width: 450px;">
			<h3 class="text-center mb-4">비밀번호 수정</h3>

			{{#msg}}
			<div class="alert alert-danger text-center">{{msg}}</div>
			{{/msg}}

			<form method="post" action="/member/modifyMemberPw">
				<div class="mb-3">
					<label for="beforePw" class="form-label">기존 비밀번호</label>
					<input type="password" name="beforePw" id="beforePw" class="form-control">
				</div>
				<div class="mb-3">
					<label for="newPw" class="form-label">변경할 비밀번호</label>
					<input type="password" name="newPw" id="newPw" class="form-control">
				</div>
				<div class="mb-3">
					<label for="confirmPw" class="form-label">비밀번호 재확인</label>
					<input type="password" name="confirmPw" id="confirmPw" class="form-control">
				</div>
				<button type="submit" class="btn btn-primary w-100">비밀번호 변경</button>
			</form>
		</div>
	</div>

	{{>layouts/footer}}

</body>
</html>